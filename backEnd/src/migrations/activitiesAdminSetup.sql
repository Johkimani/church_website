-- Activity admin permissions seeding
-- This script assumes tables: permissions, role_permissions, roles, members, member_roles already exist.

-- Note: The application also attempts to seed permissions dynamically in setupCommunityDatabase.

